#!/bin/bash

mkdir -p config/nvim
rsync -a \
    --exclude 'init.vim.backup' \
    --exclude 'lazy-lock.json' \
    --exclude 'lua/plugins/local_settings.lua' \
    --exclude '.repro' \
    ~/.config/nvim/ \
    config/nvim/
